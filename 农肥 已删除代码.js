            /* {record.tjzt == 1 ? (
              <a type="primary" size="small" onClick={() => {
                this.setState(
                  {visible: true,pageNo:1,selectedKey:`${this.numberTo(startMonth)}`,tabLangth:`${this.numberTo(startMonth)}`},
                  ()=>{
                  this.getFarmPurchaseInfo()
                });
                // this.setState({ ncmc:record.ncmc })
                this.state.ncmc = record.ncmc
                this.setState({currentrow : record})
              }}>
                编辑查看
              </a>  
               
            ): (
                <span></span>
            )} */
            /* {record.zlcid != -1 && record.tjzt == 0 ? (
              <a  type="ghost" size="small" target="_blank" href={`/spa/workflow/static4form/index.html#/main/workflow/req?requestid=${record.zlcid}&ismonitor=1`}
              >
                查看
              </a>  
              ): (
              <span></span>
            )} */




                      /* <span>提交状态：</span>
        // <WeaSelect
        //   style={{ width: '10%', marginRight: '10%'}}
        //   options={this.state.options}
        //   value={this.state.value}
          // viewAttr={this.state.viewAttr}
        //   onChange={this.onChangeSelect}
        /> */


             // <span>提交状态：</span>
        // <WeaSelect
        //   style={{ width: '10%', marginRight: '10%' }}
        //   options={this.state.options}
        //   value={this.state.value}
        //   // viewAttr={this.state.viewAttr}
        //   onChange={this.onChangeSelect}
        // />